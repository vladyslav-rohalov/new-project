import { Container, IconDarkMode, IconLightMode } from './colorMode.styled';

export default function ColorMode() {
  return (
    <Container>
      <IconDarkMode />
      <IconLightMode />
    </Container>
  );
}
